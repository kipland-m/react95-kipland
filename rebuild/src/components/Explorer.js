import React from 'react';
import styled from 'styled-components';
import { Modal, Frame } from '@react95/core';

const ContentWrapper = styled.div`
  display: flex;
  flex-wrap: wrap;
`

function Explorer({ closeExplorer }) {
  return (
    <div>
      <Modal>
        hi
      </Modal>
    </div>
  );
}

export default Explorer;
